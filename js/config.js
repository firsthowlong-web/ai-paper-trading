// ============================================================
// config.js — ตั้งค่าก่อนใช้งาน (แก้ไข 2 ค่านี้)
// ============================================================

const CONFIG = {
  // Apps Script Web App URL — copy มาจาก Deploy > Manage deployments
  SCRIPT_URL: 'https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec',

  // Shared secret token — ต้องตรงกับ API_TOKEN ใน Script Properties
  TOKEN: 'YOUR_SECRET_TOKEN_HERE',
};
